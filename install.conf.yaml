- clean: ['~']

- shell:
    -
        command: ./install -c install.conf.core.yaml
        description: Install core dotfiles
        stdin: true
        stdout: true
    -
        command: ./install -c install.conf.omz.yaml
        description: Install core dotfiles
        stdin: true
        stdout: true